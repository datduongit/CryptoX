specVersion: "v1"
serviceId: "cryptox"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "cryptox-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
